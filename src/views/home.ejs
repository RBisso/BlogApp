<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MyBlog</title>
</head>
<body>
    <% posts.forEach(post => { %>
        <div>
            <h2><%= post.title %></h2>
            <p><%= post.abstract %></p>
            <button>Read more</button>
        </div>
   <% }); { %>
        
</body>
</html>