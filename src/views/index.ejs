<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <div>
        <p>Hello world</p>
    </div>
    <div>
        <% posts.forEach(post => { %>
            <div style = "margin: 15px;">
                <form action="post/<%= post.title %>">
                    <h2><%= post.title %></h2>
                    <p><%= post.abstract %></p>
                    <button style="font-size: 12px;" type="submit">Read more</button>
                </form>
            </div>
        <% });%>
    </div>
</body>
</html>